<!-- Sidebar -->

<aside class="large-3 columns">

  <h5>Categories</h5>
  <ul class="side-nav">
    <li><%= link_to 'Articles', posts_path %></li>
    <li><%= link_to 'Ruby on Rails', posts_path(:tag => 'Ruby on Rails') %></li>
    <li><%= link_to 'Design', posts_path(tag: 'Design') %></li>
    <li><%= link_to 'HTML/CSS', posts_path(tag: 'HTML/CSS') %></li>
    <li><%= link_to 'Javascript', posts_path(tag: 'Javascript') %></li>
  </ul>

  <div class="panel">
    <h5>Events</h5>
    <div class="feed">

      <%= @eventdata.each do || %>
      <%= puts  %>
      <% end %>
    </div>
    <a href="#">Read More →</a>
  </div>

</aside>
if response.code == "200"
result = JSON.parse(response.body)
result.each do |doc|
puts doc["id"] #reference properties like this
puts doc # this is the result in object form
puts ""
puts ""
end
else
puts "ERROR!!!"
end
html +=  entry.group.name + '<br/>'; 
              html +=  entry.name + '<br/>'; 
              html +=  entry.time + '<br/>';//need to do something to make the time display in a readable format
              html +=  entry.venue.name + '<br/>';
              html +=  entry.venue.addres_1 + '<br/>';
              html +=  entry.venue.addres_2 + '<br/>';
              html += '<a href="' + entry.event_url + '">Details</a><br/>';
<!-- End Sidebar -->



